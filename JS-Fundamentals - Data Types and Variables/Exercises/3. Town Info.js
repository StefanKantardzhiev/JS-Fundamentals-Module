function townInfo(input1, num2, num3){

    let city = input1.toString()
    let population = Number(num2);
    let area = Number(num3);
    
    console.log(`Town ${input1} has population of ${population} and area ${area} square km.`)
}